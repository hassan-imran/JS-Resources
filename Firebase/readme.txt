npm -- node package manager -- to install online packages such as firebase

npm install -g firebase-tools (here -g means global installation so you can access it from any folder) (command to install nodejs)